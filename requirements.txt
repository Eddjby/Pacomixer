# Core
numpy
librosa
soundfile
streamlit
pandas

# Optional analyzer enhancements (extract_library.py auto-detects these).
# The analyzer falls back gracefully when any is missing.
#   CLAP 512-dim embeddings on GPU
# msclap
# torch
#   essentia key detection
# essentia
#   genre tags from ID3 metadata
# mutagen
